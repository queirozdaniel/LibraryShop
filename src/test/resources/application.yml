cors:
  originPatterns: http://localhost:3000,http://localhost:8080
server:
  port: 8888
spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
spring-doc:
  paths-to-match:
    - /api/**/v1/**
    - /api/**/v2/**
  swagger-ui:
    use-root-path: true